# Архитектура сервера

Веб-приложение для визуализации всех приложений хост-сервера и виртуальных машин в виде блок-схемы.

## Возможности

### Основные функции

- 📊 **Визуализация архитектуры** - интерактивная блок-схема всех приложений сервера
- 🐳 **Docker контейнеры** - отображение с детальной информацией
- 📦 **LXD контейнеры** - контейнеры и приложения внутри них
- 🖥️ **Системные сервисы** - хост-сервисы и системные приложения
- 🔄 **Автоматическое обновление** - обновление данных каждую минуту
- 🎯 **Фильтрация** - по типам приложений (Docker/LXD/Хост) и статусу

### Детальная информация о приложениях

- 🌐 **Домены** - отображение активных и запланированных доменов
- 📡 **IP адреса** - IPv4 и IPv6 адреса контейнеров и приложений
- 🔀 **Маршрутизация** - информация о firewall NAT правилах, LXD proxy устройствах, проброшенных портах
- ✅ **Доступность** - проверка доступности приложений по URL с детальной диагностикой
- ⏱️ **Производительность** - время отклика и HTTP статус коды

### Визуализация для контроля тестирования

- 🟢 **Цветовая индикация** - зеленый (работает), оранжевый (проблемы доступности), красный (остановлен)
- 🔍 **Детальная диагностика** - информация об ошибках маршрутизации и доступности
- 📋 **Панель доменов** - отдельная панель со списком всех доменов сервера
- 📊 **Структурированная визуализация** - иерархическая схема без перекрытий узлов

## Установка

### 1. Установить зависимости

```bash
pip3 install -r requirements.txt
```

### 2. Сделать скрипты исполняемыми

```bash
chmod +x app.py app_collector.py
```

### 3. Установить как systemd service (опционально)

```bash
sudo cp app-visualizer.service /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl enable app-visualizer
sudo systemctl start app-visualizer
```

### 4. Запуск вручную

```bash
cd /home/cdto/app-visualizer
python3 app.py
```

Приложение будет доступно по адресу: `http://<IP_СЕРВЕРА>:5050`

## Использование

1. Откройте браузер и перейдите по адресу приложения
2. Используйте фильтры для показа/скрытия определенных типов приложений
3. Кликните на узел для просмотра детальной информации
4. Нажмите "Обновить" для ручного обновления данных

## Структура проекта

```
app-visualizer/
├── app.py                 # Основной Flask сервер
├── app_collector.py       # Модуль сбора информации о приложениях
├── domains_config.py      # Конфигурация доменов сервера
├── requirements.txt       # Зависимости Python
├── app-visualizer.service # Systemd service файл
├── start.sh              # Скрипт запуска с venv
├── launch-browser.sh     # Скрипт запуска с открытием браузера
├── templates/
│   └── index.html        # HTML шаблон с панелью доменов
└── static/
    ├── css/
    │   └── style.css     # Стили (включая стили для панели доменов)
    └── js/
        └── app.js        # JavaScript логика визуализации
```

## Основные компоненты

### `app_collector.py`

Модуль для сбора информации о приложениях:
- Сбор данных о Docker контейнерах (порты, IP, статус)
- Сбор данных о LXD контейнерах и приложениях внутри них
- Проверка доступности URL с детальной диагностикой
- Сбор информации о маршрутизации (firewall NAT, LXD proxy)
- Определение доменов для приложений

### `domains_config.py`

Конфигурация доменов сервера:
- Активные домены (с привязкой к контейнерам)
- Запланированные домены
- Функции для поиска доменов по имени приложения/контейнера

### Визуализация

Использует библиотеку `vis.js` с иерархическим layout:
- Прямые связи (90 градусов)
- Автоматическое предотвращение перекрытий узлов
- Цветовая индикация статусов
- Интерактивные узлы с детальной информацией

## API Endpoints

- `GET /` - Главная страница
- `GET /api/apps` - JSON данные о всех приложениях (с детальной информацией о доступности, маршрутизации, доменах)
- `GET /api/health` - Health check
- `GET /api/domains` - Список всех доменов (активных и запланированных)

## Структура данных API

### Пример ответа `/api/apps`:

```json
{
  "host_ip": "192.168.1.112",
  "applications": [
    {
      "name": "grafana",
      "type": "docker",
      "status": "running",
      "internal_ip": "172.19.0.4",
      "domains": [{"domain": "stat.cdto.group", "status": "planned"}],
      "url": "http://192.168.1.112:3000",
      "url_available": true,
      "url_check": {
        "available": true,
        "status_code": 200,
        "response_time": 45.2
      },
      "routing": {
        "firewall_nat": {"type": "DNAT", "destination": "172.19.0.4:3000"}
      }
    }
  ],
  "statistics": {...}
}
```

## Технологии

- **Backend**: Python 3 + Flask
- **Frontend**: HTML5/CSS3/JavaScript + vis.js (иерархическая визуализация сетей)
- **Сбор данных**: Docker CLI, LXC CLI, iptables
- **Проверка доступности**: urllib (HTTP HEAD запросы)

## Использование для автоматического тестирования

Приложение предназначено для контроля автоматического тестирования доступности и корректной работы приложений:

- ✅ **Визуальное отображение проблем** - оранжевые узлы показывают проблемы доступности
- 🔍 **Детальная диагностика** - информация об ошибках в детальном просмотре
- 🔀 **Контроль маршрутизации** - визуализация настроек firewall и proxy
- ⏱️ **Мониторинг производительности** - время отклика приложений
- 📊 **История проверок** - актуальные данные обновляются каждую минуту

## Развертывание

### Запуск через systemd

Приложение настроено для автоматического запуска как systemd service.

### Запуск вручную

```bash
cd /home/cdto/app-visualizer
./start.sh
```

### Ярлык на рабочем столе

Создан desktop shortcut для запуска через браузер:
- Рабочий стол: `~/Desktop/Архитектура сервера.desktop`
- Меню приложений: `~/.local/share/applications/architecture-server.desktop`
