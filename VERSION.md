# Версионирование проекта "Архитектура сервера"

## Текущая версия: v1.0.0

### История версий

#### v1.0.0 (текущая) - Детальная визуализация с контролем тестирования

**Дата:** 2025-01-17

**Основные возможности:**
- 📊 Интерактивная визуализация архитектуры сервера
- 🐳 Поддержка Docker контейнеров
- 📦 Поддержка LXD контейнеров и приложений внутри них
- 🖥️ Отображение системных сервисов хоста
- 🌐 Отображение доменов (активные и запланированные)
- 📡 IP адреса (IPv4/IPv6) для всех контейнеров
- 🔀 Информация о маршрутизации (firewall NAT, LXD proxy)
- ✅ Детальная диагностика доступности приложений
- 🎨 Цветовая индикация статусов (зеленый/оранжевый/красный)
- 📋 Панель доменов на главной странице
- 🔄 Автоматическое обновление данных (каждую минуту)
- 🎯 Фильтрация по типам приложений и статусу

**Технические детали:**
- Backend: Python 3 + Flask
- Frontend: HTML5/CSS3/JavaScript + vis.js
- Сбор данных: Docker CLI, LXC CLI, iptables
- API endpoints: `/api/apps`, `/api/domains`, `/api/health`

**Улучшения визуализации:**
- Прямые связи между узлами (90 градусов)
- Предотвращение перекрытий узлов
- Автоматическое масштабирование после стабилизации
- Иерархический layout с оптимальными расстояниями

---

## Формат версионирования

Проект использует семантическое версионирование (Semantic Versioning):
- **MAJOR** (x.0.0) - несовместимые изменения API
- **MINOR** (0.x.0) - новая функциональность с обратной совместимостью
- **PATCH** (0.0.x) - исправления ошибок с обратной совместимостью

## Теги Git

Для создания нового тега версии:

```bash
git tag -a v1.0.1 -m "Описание изменений"
git push --tags
```

Для просмотра всех тегов:

```bash
git tag -l
```

## План развития

### v1.1.0 (запланировано)
- [ ] История изменений статусов приложений
- [ ] Экспорт отчетов в PDF/JSON
- [ ] Настраиваемые цветовые схемы

### v1.2.0 (запланировано)
- [ ] Интеграция с системами мониторинга (Prometheus, Grafana)
- [ ] Уведомления о проблемах (email, webhook)
- [ ] Дашборд с метриками
